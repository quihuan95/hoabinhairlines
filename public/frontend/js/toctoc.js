(function($) { $.toctoc = function(options) { var settings = $.extend({ headBackgroundColor: '#04245d',headTextColor: '#fff',headLinkColor: '#add8e6',bodyBackgroundColor: '#f5f5f5',bodyLinkColor: '#000',borderStyle: 'solid',borderColor: '#04245d',borderWidth: '1px',headText: 'Nội dung bài viết',headLinkText: ['[show]', '[hide]'],minimized: false,target: 'body' }, options);const container = $('#toctoc');const head = $("<div id='toctoc-head'></div>");const body = $("<div id='toctoc-body'></div>");if ($(".contentnewsdt h2").length > 0) { createToc();toggleLink();}$('#toctoc #toggle').on('click', () => { toggleLink(); }); function createToc() { var activeurl = window.location;head.append("<p><span> <i class='fa fa-bars'></i>&nbsp;" + settings.headText + "</span> <a href='javascript:void(0)' id='toggle'></a></p>"); let titles = settings.target + " h2, " + settings.target + " h3, " + settings.target + " h4, " + settings.target + " h5, " + settings.target + " h6"; $(titles).each(function(i) { let tag = $(this).prop('tagName').toLowerCase();let content = $(this).html();let slugs=fnSlug(content);let anchor = slugs;$(this).attr('id', anchor);body.append("<a href='"+activeurl+"#"+anchor+"'><p class='link link-"+tag+"'>"+content+"</p></a>");});container.append(head);container.append(body);customize(); }function fnSlug(v){ var title, slug;title = v;slug = title.toLowerCase();slug = slug.replace(/á|à|ả|ạ|ã|ă|ắ|ằ|ẳ|ẵ|ặ|â|ấ|ầ|ẩ|ẫ|ậ/gi, 'a');slug = slug.replace(/é|è|ẻ|ẽ|ẹ|ê|ế|ề|ể|ễ|ệ/gi, 'e');slug = slug.replace(/i|í|ì|ỉ|ĩ|ị/gi, 'i');slug = slug.replace(/ó|ò|ỏ|õ|ọ|ô|ố|ồ|ổ|ỗ|ộ|ơ|ớ|ờ|ở|ỡ|ợ/gi, 'o');slug = slug.replace(/ú|ù|ủ|ũ|ụ|ư|ứ|ừ|ử|ữ|ự/gi, 'u');slug = slug.replace(/ý|ỳ|ỷ|ỹ|ỵ/gi, 'y');slug = slug.replace(/đ/gi, 'd');slug = slug.replace(/\`|\~|\!|\@|\#|\||\$|\%|\^|\&|\*|\(|\)|\+|\=|\,|\.|\/|\?|\>|\<|\'|\"|\:|\;|_/gi, '');slug = slug.replace(/ /gi, "-");slug = slug.replace(/\-\-\-\-\-/gi, '-');slug = slug.replace(/\-\-\-\-/gi, '-');slug = slug.replace(/\-\-\-/gi, '-');slug = slug.replace(/\-\-/gi, '-');slug = '@' + slug + '@';slug = slug.replace(/\@\-|\-\@|\@/gi, '');return slug;}function customize() { head.css({ 'background-color': settings.headBackgroundColor,'color': settings.headTextColor });body.css({ 'background-color': settings.bodyBackgroundColor,'border-style': settings.borderStyle,
'border-color': settings.borderColor,'border-width': settings.borderWidth });$('#toctoc-head a').css({'color': settings.headLinkColor});$('#toctoc-body a').css({'color': settings.bodyLinkColor}); }function toggleLink() {if (settings.minimized) {settings.minimized = false;$('#toctoc-head a').text(settings.headLinkText[0]);body.addClass('hidden');} else {settings.minimized = true;$('#toctoc-head a').text(settings.headLinkText[1]);body.removeClass('hidden'); }}};})(jQuery);
